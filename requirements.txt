# Core dependencies (required for all deployments)
accelerate
json-repair
litellm
loguru
mdpd
numpy
pandas
pdf2image
PyMuPDF
python-dotenv
python-levenshtein==0.27.1
requests
setuptools
tabulate
tenacity
transformers>=4.51.1,<4.53.0
types-requests
vllm==0.8.3
xgrammar==0.1.17
Pillow

# RunPod deployment (required for serverless)
runpod>=1.7.0

# UI/Development only (not needed for RunPod deployment)
gradio==5.23.2

# Additional system dependencies may be required:
# - poppler-utils (for pdf2image)
# - For GPU support: CUDA toolkit matching vllm requirements
